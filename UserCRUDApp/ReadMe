Run npm install

go to the config file in the components folder and,
fill in the "firebaseConfig" const

run "npm install" in your turminal

set up expo with "sudo npm install -g expo-cli"

run the app with npm start

press a to open the android version
press i to open the iOS version
